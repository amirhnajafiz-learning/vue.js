// Using if else with v-if and v-else
const myapp = {
    data: function() {
        return {
            text: "racecars",
            visible: true
        }
    },
}

Vue.createApp(myapp).mount("#app");